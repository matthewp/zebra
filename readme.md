# Zebra

A class-based, convention-driven view framework with minimal library code. The framework is mostly a [skill](skills/zebra/SKILL.md) — strict conventions enforced by your AI coding agent, with a tiny runtime.

## Install the skill

```bash
npx skills add matthewp/zebra
```

## Install the library

```bash
npm install @matthewp/zebra
```
